#include "player.h"

namespace PTerminal {

Choose_Moves play_choose(Choose_Moves moves) {
    uint8_t end = static_cast<uint8_t>(CHOOSE_MOVE::SENTINEL);
    for (uint8_t i=0; i < end; i++) {
        
    }
}

}
